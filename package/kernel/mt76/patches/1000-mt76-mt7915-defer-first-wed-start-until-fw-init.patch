--- a/mt7915/dma.c
+++ b/mt7915/dma.c
@@ -252,6 +252,26 @@ static void mt7915_dma_prefetch(struct m
 	}
 }
 
+int mt7915_wed_start(struct mt7915_dev *dev)
+{
+	u32 wed_irq_mask = dev->mt76.mmio.irqmask |
+			   MT_INT_TX_DONE_BAND0 |
+			   MT_INT_TX_DONE_BAND1;
+	int ret;
+
+	if (!is_mt798x(&dev->mt76))
+		mt76_wr(dev, MT_INT_WED_MASK_CSR, wed_irq_mask);
+	else
+		mt76_wr(dev, MT_INT_MASK_CSR, wed_irq_mask);
+
+	ret = mt7915_mcu_wed_enable_rx_stats(dev);
+	if (ret)
+		return ret;
+
+	mtk_wed_device_start(&dev->mt76.mmio.wed, wed_irq_mask);
+	return 0;
+}
+
 int mt7915_dma_start(struct mt7915_dev *dev, bool reset, bool wed_reset)
 {
 	struct mt76_dev *mdev = &dev->mt76;
@@ -307,20 +327,15 @@ int mt7915_dma_start(struct mt7915_dev *
 		irq_mask |= MT_INT_BAND1_RX_DONE;
 
 	if (mtk_wed_device_active(&dev->mt76.mmio.wed) && wed_reset) {
-		u32 wed_irq_mask = irq_mask;
 		int ret;
 
-		wed_irq_mask |= MT_INT_TX_DONE_BAND0 | MT_INT_TX_DONE_BAND1;
-		if (!is_mt798x(&dev->mt76))
-			mt76_wr(dev, MT_INT_WED_MASK_CSR, wed_irq_mask);
-		else
-			mt76_wr(dev, MT_INT_MASK_CSR, wed_irq_mask);
-
-		ret = mt7915_mcu_wed_enable_rx_stats(dev);
-		if (ret)
-			return ret;
-
-		mtk_wed_device_start(&dev->mt76.mmio.wed, wed_irq_mask);
+		if (is_mt7915(mdev) && !dev->recovery.hw_init_done) {
+			dev->wed_early_start_deferred = true;
+		} else {
+			ret = mt7915_wed_start(dev);
+			if (ret)
+				return ret;
+		}
 	}
 
 	irq_mask = reset ? MT_INT_MCU_CMD : irq_mask;
--- a/mt7915/init.c
+++ b/mt7915/init.c
@@ -1,6 +1,7 @@
 // SPDX-License-Identifier: BSD-3-Clause-Clear
 /* Copyright (C) 2020 MediaTek Inc. */
 
+#include <linux/delay.h>
 #include <linux/etherdevice.h>
 #include <linux/hwmon.h>
 #include <linux/hwmon-sysfs.h>
@@ -847,6 +848,14 @@ mt7915_init_hardware(struct mt7915_dev *
 	if (ret)
 		return ret;
 
+	if (dev->wed_early_start_deferred) {
+		/* Let WM/WA firmware finish bootstrap before arming WED txfree. */
+		msleep(400);
+		ret = mt7915_wed_start(dev);
+		if (ret)
+			return ret;
+	}
+
 	ret = mt7915_eeprom_init(dev);
 	if (ret < 0)
 		return ret;
--- a/mt7915/mt7915.h
+++ b/mt7915/mt7915.h
@@ -300,6 +300,7 @@ struct mt7915_dev {
 	bool flash_mode;
 	bool muru_debug;
 	bool ibf;
+	bool wed_early_start_deferred;
 
 	u8 monitor_mask;
 
@@ -443,6 +444,7 @@ s8 mt7915_eeprom_get_power_delta(struct
 bool mt7915_eeprom_has_background_radar(struct mt7915_dev *dev);
 int mt7915_dma_init(struct mt7915_dev *dev, struct mt7915_phy *phy2);
+int mt7915_wed_start(struct mt7915_dev *dev);
 void mt7915_dma_prefetch(struct mt7915_dev *dev);
 void mt7915_dma_cleanup(struct mt7915_dev *dev);
 int mt7915_dma_reset(struct mt7915_dev *dev, bool force);
