From bdbdcfa2efdc414324351e595128462aaba14e1c Mon Sep 17 00:00:00 2001
From: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
Date: Wed, 24 Sep 2025 12:58:10 +0800
Subject: [PATCH] netfilter: add DSCP learning flow to nft_flow_offload

Learn the SKB DSCP into the conntrack qos extension over the first
nf_conntrack_qos packets per direction, then seed the offloaded flow
tuple tos so the PPE (via nf_flow_rule_match KEY_IP -> mtk_foe_entry_set_dscp)
programs the learned DSCP into the FOE entry.

Enable with: echo 1 > /proc/sys/net/netfilter/nf_conntrack_qos

Re-adapted to mainline 6.12.87 nft_flow_offload_eval(): mainline computes
dir after test_and_set_bit(IPS_OFFLOAD) and routes via nft_flow_route()
(no is_bridging branch), so the dir hoist and qos block are re-anchored.
Added <net/dsfield.h> for ipv6_get_dsfield() (not otherwise included).

Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
---
 net/netfilter/nft_flow_offload.c | 47 +++++++++++++++++++++++++++++++-
 1 file changed, 46 insertions(+), 1 deletion(-)

diff --git a/net/netfilter/nft_flow_offload.c b/net/netfilter/nft_flow_offload.c
--- a/net/netfilter/nft_flow_offload.c
+++ b/net/netfilter/nft_flow_offload.c
@@ -12,6 +12,8 @@
 #include <net/inet_dscp.h>
 #include <net/netfilter/nf_tables.h>
 #include <net/netfilter/nf_tables_core.h>
+#include <net/dsfield.h>
+#include <net/netfilter/nf_conntrack_qos.h>
 #include <net/netfilter/nf_conntrack_core.h>
 #include <net/netfilter/nf_conntrack_extend.h>
 #include <net/netfilter/nf_flow_table.h>
@@ -20,6 +22,19 @@
 	struct nft_flowtable	*flowtable;
 };
 
+static void
+nft_flow_offload_dscp_init(struct flow_offload *flow, const struct nf_conn *ct,
+			   enum ip_conntrack_dir dir)
+{
+	struct nf_conn_qos *qos;
+
+	qos = nf_conn_qos_find(ct);
+	if (qos) {
+		flow->tuplehash[dir].tuple.tos = qos->tos[dir].value;
+		flow->tuplehash[!dir].tuple.tos = qos->tos[!dir].value;
+	}
+}
+
 static enum flow_offload_xmit_type nft_xmit_type(struct dst_entry *dst)
 {
 	if (dst_xfrm(dst))
@@ -317,6 +332,11 @@
 	struct flow_offload *flow;
 	enum ip_conntrack_dir dir;
 	struct nf_conn *ct;
+	struct nf_conn_qos *qos;
+	struct ipv6hdr *ip6h;
+	struct iphdr *iph;
+	u32 offset = 0;
+	u8 tos = 0;
 	int ret;
 
 	if (nft_flow_offload_skip(pkt->skb, nft_pf(pkt)))
@@ -360,10 +380,32 @@
 	if (!nf_ct_is_confirmed(ct))
 		goto out;
 
+	dir = CTINFO2DIR(ctinfo);
+
+	qos = nf_conn_qos_find(ct);
+	if (qos) {
+		struct net *net = nf_ct_net(ct);
+
+		switch (nft_pf(pkt)) {
+		case NFPROTO_IPV4:
+			iph = (struct iphdr *)(skb_network_header(pkt->skb) + offset);
+			tos = iph->tos;
+			break;
+		case NFPROTO_IPV6:
+			ip6h = (struct ipv6hdr *)(skb_network_header(pkt->skb) + offset);
+			tos = ipv6_get_dsfield(ip6h);
+			break;
+		}
+
+		qos->tos[dir].value = tos;
+		if (atomic64_inc_return_relaxed(&qos->tos[dir].counter) <
+		    net->ct.sysctl_qos)
+			goto out;
+	}
+
 	if (test_and_set_bit(IPS_OFFLOAD_BIT, &ct->status))
 		goto out;
 
-	dir = CTINFO2DIR(ctinfo);
 	if (nft_flow_route(pkt, ct, &route, dir, priv->flowtable) < 0)
 		goto err_flow_route;
 
@@ -375,6 +417,8 @@
 	if (tcph)
 		flow_offload_ct_tcp(ct);
 
+	nft_flow_offload_dscp_init(flow, ct, dir);
+
 	__set_bit(NF_FLOW_HW_BIDIRECTIONAL, &flow->flags);
 	ret = flow_offload_add(flowtable, flow);
 	if (ret < 0)
